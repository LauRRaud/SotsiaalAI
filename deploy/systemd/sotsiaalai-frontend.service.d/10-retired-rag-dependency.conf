[Unit]
# The original frontend unit required the retired Python RAG service. Clear the
# inherited lists before later repository drop-ins add their current dependencies.
Requires=
After=
After=network.target postgresql.service
